import {StyleSheet} from "react-native";

export const mainColor = '#e1b497';

export const GlobalStyles = StyleSheet.create({
	title: {
		fontSize: 20,
		fontWeight: 'bold',
		color: '#333',
		margin: 10,
	},
	centeredView: {
		flex: 1,
		justifyContent: 'center',
		alignItems: 'center',
	}
});
